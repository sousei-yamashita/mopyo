# CI Gate live smoke test

This temporary documentation-only change exists to verify the CI -> workflow_run -> CI Gate path on GitHub after CI Gate was installed on the default branch.

It does not change product behavior, dependencies, build configuration, or deployment.
